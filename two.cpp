#include <iostream>
using namespace std;

int main() {
    int a, b, sum = 0;
    cout << "Enter the first number: ";
    cin >> a;
    cout << "Enter the second number: ";
    cin >> b;

    sum = a + b;
    cout << "The sum of the numbers is: " << sum << endl;

    return 0;
}
